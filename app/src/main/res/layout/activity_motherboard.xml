<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Motherboard">

    <Button
        android:id="@+id/btnRegresar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#4CAF50"
        android:text="Atras" />

    <TextView
        android:id="@+id/etmConcepto"
        android:layout_width="357dp"
        android:layout_height="322dp"
        android:layout_marginTop="150dp"
        android:layout_marginLeft="25dp"
        android:textColor="@android:color/black"
        android:textSize="18dp"
        android:ems="10"
        android:gravity="start|top"
        android:inputType="textMultiLine"
        android:text="Es llamada en algunos países la tarjeta madre o placa
         base es la parte más importante de la CPU (Unidad Central de Procesamiento), es un
          circuito muy complejo que se encarga de unir todos los componentes que se necesitan
           para poder procesar la información que la computadora recibe."/>

</RelativeLayout>